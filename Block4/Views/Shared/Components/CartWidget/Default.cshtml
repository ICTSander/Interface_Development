@page
@model Block4.Models.CartSummaryView

<section class="cart-section" id="cartSectionContainer">
    <div class="shippingCostContainer">
        <img src="/img/package.png" alt="package icon" />
        <p id="shippingCost-text"></p>
        <div id="shippingCost-progressBarContainer">
            <div id="shippingCost-progressBar"></div>
        </div>
    </div>
    <div class="cart-items-container">
        @if (Model?.CartItems == null || !Model.CartItems.Any())
        {
            <p class="empty-cart-message">Je winkelwagen is leeg</p>
        }
        else
        {
            @foreach (var item in Model.CartItems)
            {
                <div class="cart-item-conainer">
                    <img src="@item.ImageUrl" alt="product" />
                    <div class="cart-item-title">@item.ProductName</div>
                    <div class="cart-item-price">€@item.Price.ToString("F2")</div>
                    <div class="change-item-amount-container">
                        <button type="button" class="cart-item-amount-reduce" data-id="@item.ProductId">-</button>
                        <div class="cart-item-amount-text">@item.Quantity</div>
                        <button type="button" class="cart-item-amount-add" data-id="@item.ProductId">+</button>
                    </div>
                    <button type="button" class="cart-item-removeBtn" data-id="@item.ProductId">
                        <img class="cart-item-remove-img" src="@Model.RemoveImg" alt="X" />
                    </button>
                </div>
            }
        }
    </div>
    <form method="post" class="cart-item-paying-Container">
        <div class="cart-item-paying-subtotal-container">
            <div class="cart-item-paying-subtotal-text">Subtotaal</div>
            <div class="cart-item-paying-subtotal-value">€@Model.Subtotal.ToString("F2")</div>
        </div>
        <div class="cart-item-paying-shippingCost-container">
            <div class="cart-item-paying-shippingCost-text">Verzendkosten</div>
            <div class="cart-item-paying-shippingCost-value">€@Model.ShippingCost.ToString("F2")</div>
        </div>
        <hr />
        <div class="cart-item-paying-total">Totaal: €@Model.Total.ToString("F2")</div>
        <a href="/Checkout" class="cart-item-buyBtn">Afrekenen</a>
    </form>
</section>
